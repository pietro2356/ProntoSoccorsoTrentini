import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pr.prontosoccorso.trentini',
  appName: 'ProntoSoccorsoTrentini',
  webDir: 'www'
};

export default config;
